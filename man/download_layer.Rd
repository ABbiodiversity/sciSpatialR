% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_layer.R
\name{download_layer}
\alias{download_layer}
\title{Copy a catalogued layer to a local directory}
\usage{
download_layer(
  name,
  dest,
  overwrite = FALSE,
  dry_run = FALSE,
  verify = TRUE,
  keep_theme = FALSE,
  quiet = FALSE,
  ...
)
}
\arguments{
\item{name}{Character; a layer \code{name} (\code{"fab_dem"}) or catalogue
\code{id} (\code{"elevation/fab_dem"}) as listed by \code{\link[=list_layers]{list_layers()}}.}

\item{dest}{Character; a local directory to copy into.  Created
if it does not exist.}

\item{overwrite}{Logical; if \code{TRUE}, copy every file again rather
than skipping those already present and unchanged.  Default
\code{FALSE}.}

\item{dry_run}{Logical; if \code{TRUE}, report what would be copied
and how much it comes to, writing nothing.  Default \code{FALSE}.}

\item{verify}{Logical; if \code{TRUE} (default), check every copied
file against its source on size and md5 checksum, and record
the result in the log.  Verification re-reads each copied file
from the share, so it roughly doubles the time a large copy
takes; pass \code{FALSE} to skip it.}

\item{keep_theme}{Logical; if \code{TRUE}, include the layer's ISO
topic category folder in the local path, reproducing the
share's full structure.  Default \code{FALSE}.}

\item{quiet}{Logical; if \code{TRUE}, suppress progress messages.
Default \code{FALSE}.}

\item{...}{Passed to \code{\link[=build_catalogue]{build_catalogue()}}, e.g. \code{root} or
\code{refresh}.}
}
\value{
Invisibly, a \code{data.frame} with one row per file copied,
skipped, or attempted:
\describe{
\item{\code{source}}{Path on the share.}
\item{\code{destination}}{Path written under \code{dest}.}
\item{\code{bytes}}{Size of the source, summed over a
geodatabase's contents.}
\item{\code{part}}{\code{"layer"} for the layer's own files,
\code{"product"} for the parent product's metadata.}
\item{\code{status}}{\code{"copied"}, \code{"skipped"}, or \code{"failed"}; or
\code{"would copy"} and \code{"would skip"} under \code{dry_run}.}
\item{\code{verified}}{\code{"ok"}, the reason verification failed, or
\code{"not checked"}.  \code{NA} for a skipped file.}
\item{\code{md5}}{The source checksum, where one was taken.}
}
}
\description{
Copies a layer from the spatial data share to a folder of your
own, bringing its documentation with it.  Everything in the layer
folder comes down — the data, its readme, and any ancillary files
beside them — and where a product's record is split between a
product readme and a variant readme, both are copied.
}
\details{
Use this to work off the network, or to hand someone a dataset
they can cite.  To read a layer without copying it, use
\code{\link[=get_layer]{get_layer()}}.

The copy is rooted at the product folder, which lands directly in
\code{dest}: a layer catalogued as \code{geoscientific/soilgrids/soil1km}
arrives as \verb{<dest>/soilgrids/soil1km}, with the product's readme
one level above it in \verb{<dest>/soilgrids}, exactly as the two sit
on the share.  The ISO topic category is the share's filing
system rather than part of the dataset, so it is left behind;
pass \code{keep_theme = TRUE} to reproduce the full catalogue path,
which is what keeps two same-named products in different themes
from merging into one folder.  Sibling variants of the same
product are separate layers and are not copied; ask for them by
name.

Only the product's \emph{non-data} files are copied alongside a
variant — its readme and any documentation, not a second copy of
any data file that happens to sit at product level.  Shapefile
sidecars (\code{.dbf}, \code{.shx}, \code{.prj} and friends) are treated as data
and always accompany their \code{.shp}, unlike in \code{\link[=layer_files]{layer_files()}},
which hides them so a shapefile is listed once.  An Esri file
geodatabase is copied whole, as one item.

A file already present in \code{dest} and matching its source on size
and modification time is skipped, so an interrupted copy resumes
rather than restarts.  \code{overwrite = TRUE} forces every file to be
copied again; \code{overwrite = FALSE} still replaces a destination
file that differs from its source.

Every copy leaves a receipt: \verb{_download_log.txt} is written into
the layer's folder alongside the data, recording what was copied,
from where, by whom, when, how long it took, and each file's size
and md5 checksum.  Runs append to it rather than replacing it, so
a layer topped up over several sessions keeps its whole history.
A copy whose provenance lives only in the console scrollback has
none by the time anyone asks, which is the same reason the readme
comes down with the data.

With \code{verify = TRUE} (the default) each copied file is read back
and compared to its source on size and md5 checksum before the
log records it as sound.  Only files copied in that run are
checked: a skipped file was verified when it was copied, and the
log beside it says so, which keeps a re-run cheap.

Sidecar-style metadata such as a raster's \code{.tif.aux.xml} has
extension \code{xml} and so counts as ancillary rather than data.  At
product level that can bring down an orphan \code{.aux.xml}, which is
harmless: it is metadata, and it is tiny.
}
\examples{
\dontrun{
# See what a copy would cost before starting one.
download_layer("fab_dem", "D:/local_spatial", dry_run = TRUE)

download_layer("fab_dem", "D:/local_spatial")

# A variant brings its product's readme down with it.  The
# product name resolves on its own while it holds one variant;
# otherwise give the full id list_layers() reports.
p <- download_layer("soilgrids_250_v2_ab", "D:/local_spatial")
p[p$part == "product", c("destination", "status")]

# Reproduce the share's full path, theme folder included.
download_layer("fab_dem", "D:/local_spatial", keep_theme = TRUE)

# The receipt left beside the data.
cat(readLines("D:/local_spatial/fab_dem/_download_log.txt"),
    sep = "\n")

# Skip the read-back on a very large copy.
download_layer("scanfi", "D:/local_spatial", verify = FALSE)

# Re-running copies only what changed.
download_layer("fab_dem", "D:/local_spatial")
}

}
\seealso{
\code{\link[=get_layer]{get_layer()}} to read a layer without copying it,
\code{\link[=layer_files]{layer_files()}} to see what a layer folder holds, and
\code{\link[=layer_meta]{layer_meta()}} for its metadata.
}
