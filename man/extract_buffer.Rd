% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_buffer.R
\name{extract_buffer}
\alias{extract_buffer}
\title{Extract a summary statistic within one or more buffer radii}
\usage{
extract_buffer(x, points, radii, fun = mean, fun_name = NULL, bind = TRUE, ...)
}
\arguments{
\item{x}{A \code{SpatRaster} with one or more layers.}

\item{points}{An \code{sf} or \code{SpatVector} of point locations.}

\item{radii}{Numeric vector of buffer radii in CRS units.}

\item{fun}{Summary function; passed to \code{\link[terra:extract]{terra::extract()}} via
the \code{fun} argument.  Default \code{mean}.}

\item{fun_name}{Character label appended to column names.  If
\code{NULL} (default), the name of \code{fun} is used when \code{fun} is a
named function, or \code{"stat"} otherwise.}

\item{bind}{Logical; if \code{TRUE} (default), columns are
bound to the attributes of \code{points}.}

\item{...}{Additional arguments passed to \code{\link[terra:extract]{terra::extract()}}.}
}
\value{
A \code{data.frame} with one row per point.  Extracted
columns are named \verb{<layer>_r<radius>} (with the summary
function name omitted for brevity unless multiple functions
are requested in separate calls).
}
\description{
For each combination of point, raster layer, and buffer radius,
computes a summary statistic over the cell values within the
circular buffer.  Vectorised over \code{radii} so all results are
returned in a single \code{data.frame}.  Output columns are named
\verb{<layer>_r<radius>} (where radius is in CRS units).
}
\examples{
\dontrun{
library(terra)
library(sf)
r   <- rast(nrows = 100, ncols = 100, crs = "EPSG:3400",
            xmin = 0, xmax = 10000,
            ymin = 0, ymax = 10000)
r[] <- runif(ncell(r))
pts <- st_as_sf(
  data.frame(id = 1:2, x = c(2500, 7500), y = c(5000, 5000)),
  coords = c("x", "y"), crs = 3400
)
extract_buffer(r, pts, radii = c(250, 500, 1000))
}

}
