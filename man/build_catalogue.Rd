% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue.R
\name{build_catalogue}
\alias{build_catalogue}
\title{Build the catalogue manifest by scanning the data share}
\usage{
build_catalogue(
  root = spatial_root(),
  files = TRUE,
  refresh = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{root}{Character; directory to scan.  Defaults to
\code{\link[=spatial_root]{spatial_root()}}.}

\item{files}{Logical; if \code{TRUE} (default), inventory each layer's
files to populate \code{n_files}, \code{size_mb}, and \code{data_type}.  Set
\code{FALSE} for a faster, metadata-only scan.}

\item{refresh}{Logical; if \code{TRUE}, rescan even when a cached
manifest exists.  Default \code{FALSE}.}

\item{quiet}{Logical; if \code{TRUE}, suppress progress messages.
Default \code{FALSE}.}
}
\value{
A \code{data.frame} with one row per layer — one per variant
where a product has them, otherwise one per product.  Columns
are \code{id} (path relative to \code{root}), \code{name} (the product folder),
\code{theme}, \code{product_id}, \code{variant} (\code{NA} when the product has no
variant folders), the template metadata fields (see
\code{\link[=as_metadata_row]{as_metadata_row()}}), \code{n_files}, \code{size_mb}, \code{data_type}, \code{path}
(the folder holding the data), \code{readme} (the record defining
the row), and \code{product_readme} (equal to \code{readme} unless the
record is split).  Carries the scan \code{root} and the
\code{undocumented} folders as attributes.
}
\description{
Walks \code{root}, parses every dataset readme it finds, and returns
one row per documented layer.  Folders holding spatial data with
no readme are recorded separately in the \code{undocumented}
attribute and reported by \code{\link[=check_metadata]{check_metadata()}}.
}
\details{
The top-level \verb{_temp} folder is skipped: it is the share's
scratch area, so its contents are neither catalogued as layers
nor reported as missing readmes.

The result is cached per root for the session, so \code{\link[=list_layers]{list_layers()}}
and friends only pay for the scan once.
}
\examples{
\dontrun{
cat_df <- build_catalogue()
nrow(cat_df)
build_catalogue(refresh = TRUE, files = FALSE)
}

}
\seealso{
\code{\link[=list_layers]{list_layers()}} to query the manifest, \code{\link[=check_metadata]{check_metadata()}}
to audit it.
}
