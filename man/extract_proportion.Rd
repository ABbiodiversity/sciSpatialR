% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_proportion.R
\name{extract_proportion}
\alias{extract_proportion}
\title{Extract class proportions within a buffer for categorical layers}
\usage{
extract_proportion(x, points, radius, bind = TRUE, ...)
}
\arguments{
\item{x}{A categorical \code{SpatRaster} (single layer).}

\item{points}{An \code{sf} or \code{SpatVector} of point locations.}

\item{radius}{Numeric; buffer radius in the CRS units of \code{x}.}

\item{bind}{Logical; if \code{TRUE} (default), the proportions are
column-bound to the attributes of \code{points}.}

\item{...}{Additional arguments passed to \code{\link[terra:extract]{terra::extract()}}.}
}
\value{
A \code{data.frame} with one row per point and one proportion
column per class.
}
\description{
For each point location, draws a circular buffer of \code{radius}
(in CRS units) and computes the proportion of each class in a
categorical \code{SpatRaster}.  All points share the same set of
class columns; classes absent within a buffer are filled with
zero.
}
\examples{
\dontrun{
library(terra)
library(sf)
r   <- rast(nrows = 100, ncols = 100, crs = "EPSG:3400",
            xmin = 0, xmax = 10000,
            ymin = 0, ymax = 10000)
r[] <- sample(1:3, ncell(r), replace = TRUE)
pts <- st_as_sf(
  data.frame(id = 1:2, x = c(2500, 7500), y = c(5000, 5000)),
  coords = c("x", "y"), crs = 3400
)
extract_proportion(r, pts, radius = 500)
}

}
