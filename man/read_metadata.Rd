% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{read_metadata}
\alias{read_metadata}
\title{Read a spatial metadata readme}
\usage{
read_metadata(path)
}
\arguments{
\item{path}{Character; path to a readme file.}
}
\value{
A named list of length-one character values with class
\code{sciSpatial_metadata}, carrying the source \code{path} and the
template \code{sections} found as attributes.  Fields defined in
the template but absent from the file are returned as \code{NA}.
}
\description{
Parses one \code{readme.txt} or \code{readme.md} written to the ABMI
spatial metadata template into a named list of fields.  Field
labels are normalised to \code{snake_case} keys, so
\verb{West Bounding Coordinate:} becomes \code{west_bounding_coordinate}
and sub-fields indented under a parent label are prefixed with
it (\code{extent_west_bounding_coordinate}).
}
\details{
Every \code{Label: value} line is captured, including fields the
template does not define, so nothing in a readme is silently
dropped.  Unfilled template placeholders such as \verb{[Data Title]}
and non-values such as \code{"Not Specified"} are returned as \code{NA}.
}
\examples{
\dontrun{
md <- read_metadata(
  file.path(spatial_root(), "elevation", "fab_dem", "readme.txt")
)
md$title
md$extent_west_bounding_coordinate
}

}
\seealso{
\code{\link[=layer_meta]{layer_meta()}} to read metadata by layer name,
\code{\link[=as_metadata_row]{as_metadata_row()}} to flatten it to a one-row \code{data.frame},
and \code{\link[=check_metadata]{check_metadata()}} to audit completeness.
}
