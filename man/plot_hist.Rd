% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_hist.R
\name{plot_hist}
\alias{plot_hist}
\title{Plot a histogram of raster cell values}
\usage{
plot_hist(
  x,
  bins = 50,
  col = NULL,
  border = NA,
  main = NULL,
  xlab = "Pixel value",
  ylab = "Count",
  legend = FALSE,
  maxcell = 1e+06,
  ...
)
}
\arguments{
\item{x}{A \code{SpatRaster}, or the path to a single raster file.}

\item{bins}{Number of bins.  Default \code{50}.}

\item{col}{Character vector of colours the bar fill ramps
through, low value to high.  Defaults to a reversed
"Hiroshige" ramp.  Bars are filled by where their bin sits in
the layer's own range, so each facet ramps through the whole
palette.}

\item{border}{Colour of the bar outlines.  Default \code{NA}, no
outline.}

\item{main}{Plot title.  Defaults to the layer name for a
single-layer raster, and to none for a facetted one, where the
strips carry the names.}

\item{xlab, ylab}{Axis labels.  Default \code{"Pixel value"} and
\code{"Count"}.}

\item{legend}{Logical; draw the fill legend.  Default \code{FALSE},
since the fill repeats what the x axis already shows.  When
drawn it is labelled on 0 to 1, the bin's relative position in
its layer's range.}

\item{maxcell}{Numeric; the raster is downsampled to this many
cells before the histogram is computed.  Default \code{1e6}.}

\item{...}{Additional arguments passed to
\code{\link[ggplot2:geom_histogram]{ggplot2::geom_histogram()}}, e.g. \code{boundary} or \code{alpha}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the distribution of valid (non-\code{NA}) cell values in \code{x}
with ggplot2, bars filled along the same value ramp used by
\code{\link[=plot_raster]{plot_raster()}}.  Multi-layer rasters are facetted, each panel
scaled to its own layer.
}
\details{
The raster is downsampled to at most \code{maxcell} cells before the
histogram is computed, so it approximates the distribution of a
large layer.

The default colours are the "Hiroshige" palette from MetBrewer,
reversed so low values are dark blue and high values red, and
the default theme is \code{\link[=theme_science_map]{theme_science_map()}}.  Both are ordinary
ggplot2 components, so either can be replaced by adding a new
\verb{scale_fill_*()} or theme to the returned plot.
}
\examples{
\dontrun{
library(terra)
r <- get_layer("fab_dem")

plot_hist(r)
plot_hist(r, bins = 100)

# A ggplot object, so it composes and saves as one
p <- plot_hist(r) + ggplot2::scale_x_log10()
ggplot2::ggsave("2_pipeline/fab_dem_hist.png", p,
                width = 8, height = 5, dpi = 200)
}

}
\seealso{
\code{\link[=plot_raster]{plot_raster()}} for the spatial view of the same
values; \code{\link[=raster_stats]{raster_stats()}} for them as a table;
\code{\link[=theme_science_map]{theme_science_map()}} for the theme applied.
}
