% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_raster.R
\name{plot_raster}
\alias{plot_raster}
\title{Plot a raster with a stretched colour scale and boundary}
\usage{
plot_raster(
  x,
  stretch = c(0.02, 0.98),
  col = NULL,
  boundary = "alberta",
  boundary_col = "grey20",
  boundary_lwd = 0.4,
  na_col = NA,
  main = NULL,
  legend_title = NULL,
  axes = FALSE,
  maxcell = 5e+05,
  ...
)
}
\arguments{
\item{x}{A \code{SpatRaster}, or the path to a single raster file.}

\item{stretch}{Numeric vector of two probabilities giving the
quantiles the colour scale is clamped to.  Default
\code{c(0.02, 0.98)}; values beyond them take the end colours.
\code{NULL} uses the full value range, as does a layer whose two
quantiles coincide.}

\item{col}{Character vector of colours for the value scale.
Defaults to a 256-step reversed "Hiroshige" ramp.}

\item{boundary}{Polygon drawn over the raster: a \code{SpatVector},
an \code{sf} object, or one of the shortcuts accepted by
\code{\link[=mask_to_boundary]{mask_to_boundary()}}.  Defaults to \code{"alberta"}, the packaged
provincial boundary; \code{NULL} draws no overlay.}

\item{boundary_col}{Colour of the boundary outline.  Default
\code{"grey20"}.}

\item{boundary_lwd}{Width of the boundary outline, as ggplot2
\code{linewidth}.  Default \code{0.4}.}

\item{na_col}{Fill for \code{NA} cells.  Default \code{NA}, leaving them
unpainted.  Set to e.g. \code{"firebrick2"} to see which cells are
missing.}

\item{main}{Plot title.  Defaults to the layer name for a
single-layer raster, and to none for a facetted one, where the
strips carry the names.}

\item{legend_title}{Title for the fill legend.  Default \code{NULL},
no title.}

\item{axes}{Logical; draw coordinate axes.  Default \code{FALSE}.}

\item{maxcell}{Numeric; the raster is downsampled to this many
cells before plotting.  Default \code{5e5}.}

\item{...}{Additional arguments passed to
\code{\link[ggplot2:geom_tile]{ggplot2::geom_tile()}}, e.g. \code{alpha}.}
}
\value{
A \code{ggplot} object.
}
\description{
Maps \code{x} with ggplot2, clamping the colour scale to central
quantiles of the values and overlaying a boundary polygon.
Intended as a quick visual check of a harmonized or exported
layer: whether the spatial pattern looks right, and whether the
layer is clipped to the boundary it should be.
}
\details{
The raster is downsampled to at most \code{maxcell} cells before
plotting, so stretch limits and the drawn surface are
approximations for large layers.  Cells are drawn as tiles sized
from the raster's own resolution, so a masked layer's gaps stay
gaps.  Multi-layer rasters are drawn as facets sharing one
colour scale, stretched on the pooled values; pass a single
layer (\code{x[[1]]}) when each layer needs its own scale.

Unprojected (longitude/latitude) rasters are given a
latitude-corrected aspect ratio; projected rasters are drawn
with equal axes.

The default colours are the "Hiroshige" palette from MetBrewer,
reversed so low values are dark blue and high values red, and
the default theme is \code{\link[=theme_science_map]{theme_science_map()}}.  Both are ordinary
ggplot2 components, so either can be replaced by adding a new
\verb{scale_fill_*()} or theme to the returned plot.
}
\examples{
\dontrun{
library(terra)
r <- get_layer("fab_dem")

plot_raster(r)                       # stretched, Alberta outline
plot_raster(r, stretch = NULL)       # full value range
plot_raster(r, na_col = "firebrick2")  # highlight missing cells
plot_raster(r, boundary = my_study_area)

# A ggplot object, so it composes and saves as one
p <- plot_raster(r, main = "FABDEM") +
  ggplot2::labs(caption = "1 km, EPSG:3400")
ggplot2::ggsave("2_pipeline/fab_dem.png", p,
                width = 9, height = 6, dpi = 200)
}

}
\seealso{
\code{\link[=plot_hist]{plot_hist()}} for the value distribution behind the
map; \code{\link[=raster_stats]{raster_stats()}} for the same values as a table;
\code{\link[=theme_science_map]{theme_science_map()}} for the theme applied.
}
