% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_alignment.R
\name{check_alignment}
\alias{check_alignment}
\title{Check alignment of a spatial layer against a reference grid}
\usage{
check_alignment(x, ref = ab_grid(), tol = 1e-06, verbose = TRUE)
}
\arguments{
\item{x}{A \code{SpatRaster} (terra) or \code{sf} object to check.}

\item{ref}{A \code{SpatRaster} used as the reference grid.  Defaults
to the ABMI 1 km Alberta grid, \code{\link[=ab_grid]{ab_grid()}}.}

\item{tol}{Numeric tolerance used for extent, resolution, and
origin comparisons.  Defaults to \code{1e-6}.}

\item{verbose}{Logical; if \code{TRUE} (default), messages are printed
for each property.}
}
\value{
A named logical vector (invisible) with elements \code{crs},
\code{extent}, \code{resolution}, and \code{origin}.  \code{TRUE} indicates
agreement with \code{ref}.
}
\description{
Tests whether \code{x} shares the same CRS, extent, resolution, and
origin as \code{ref}.  A named logical vector is returned (invisibly)
with one element per property.  Mismatches trigger an informative
message so callers can decide whether to harmonise before
proceeding.
}
\examples{
\dontrun{
library(terra)
ref <- rast(nrows = 10, ncols = 10, crs = "EPSG:3400")
r   <- rast(nrows = 10, ncols = 10, crs = "EPSG:4326")
check_alignment(r, ref)

# Against the default ABMI 1 km Alberta grid
check_alignment(r)
}

}
