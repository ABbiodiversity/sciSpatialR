% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_vector.R
\name{extract_vector}
\alias{extract_vector}
\title{Point-in-polygon attribute join}
\usage{
extract_vector(points, polygons, cols = NULL, ...)
}
\arguments{
\item{points}{An \code{sf} object (point geometry).}

\item{polygons}{An \code{sf} polygon layer with attributes to join.}

\item{cols}{Character vector of column names from \code{polygons} to
retain.  \code{NULL} (default) retains all non-geometry columns.}

\item{...}{Additional arguments passed to \code{\link[sf:st_join]{sf::st_join()}}.}
}
\value{
An \code{sf} object with the same geometry as \code{points} and
additional columns from \code{polygons}.
}
\description{
Joins attributes from a polygon layer to point locations by
spatial overlay.  Each point receives the attributes of the
polygon it falls within.  Points outside all polygons receive
\code{NA} for the joined columns.  Commonly used to attach natural
subregion, LUF zone, ownership, or watershed identifiers.
}
\examples{
\dontrun{
library(sf)
pts  <- st_as_sf(data.frame(id = 1, x = -114, y = 53),
                 coords = c("x", "y"), crs = 4326)
poly <- st_read("natural_subregions.gpkg")
pts_joined <- extract_vector(pts, poly, cols = "NRNAME")
}

}
