% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variables.R
\name{list_variables}
\alias{list_variables}
\title{List the variables a product publishes}
\usage{
list_variables(theme = NULL, product = NULL, verbose = TRUE, ...)
}
\arguments{
\item{theme}{Optional character; theme folder name(s) to keep,
e.g. \code{"elevation"}.  Matched case-insensitively.}

\item{product}{Optional character; product name(s) or catalogue
id(s), e.g. \code{"soilgrids_250_v2_ab"} or
\code{"geoscientificInformation/soilgrids_250_v2_ab"}.  Matched
case-insensitively: exactly first, and if nothing matches
exactly, as a substring, so a fragment of a long product name
is enough.}

\item{verbose}{Logical; if \code{TRUE} (default), print a compact
summary and return the table invisibly.}

\item{...}{Passed to \code{\link[=build_catalogue]{build_catalogue()}}, e.g. \code{root},
\code{refresh}, or \code{files}.}
}
\value{
A \code{data.frame} with one row per band: \code{product} (the
product's catalogue id), \code{name}, \code{theme}, \code{band}, \code{measure},
\code{units}, \code{scale}, \code{valid_range}, and \code{description}.  Classed
\code{sciSpatial_variables} unless \code{verbose = FALSE}.
}
\description{
Reports the bands documented in each product's readme — what
each one measures, its units, its measurement scale, and its
valid range — so the variables available for extraction can be
browsed without opening a readme or a raster.
}
\details{
Bands belong to the product, not to a variant: the data
management guide records them once in the product readme, since
resampling a layer does not change what its bands measure.  A
product with several variants therefore contributes one set of
rows, not one set per variant.

Products whose readme documents no bands are listed with \code{band}
\code{NA} rather than dropped, so a documentation gap is visible
instead of looking like an absent product.  \code{\link[=check_metadata]{check_metadata()}}
reports the same gaps against the required template fields.
}
\examples{
\dontrun{
list_variables()
list_variables(theme = "geoscientificInformation")
list_variables(product = "soilgrids")

# Every band measuring pH, across the catalogue.
v <- list_variables(verbose = FALSE)
v[grepl("pH", v$measure, ignore.case = TRUE), ]
}

}
\seealso{
\code{\link[=list_layers]{list_layers()}} for the layers themselves,
\code{\link[=layer_meta]{layer_meta()}} for one layer's full metadata.
}
