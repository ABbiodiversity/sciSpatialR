% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reference.R
\name{ab_grid}
\alias{ab_grid}
\title{ABMI 1 km reference grid}
\usage{
ab_grid()
}
\value{
A \code{SpatRaster} with a single layer named \code{grid_1km}.
}
\description{
Returns the ABMI 1 km reference grid for Alberta as a template
\code{SpatRaster}: 1234 rows by 695 columns of 1000 m cells in
\code{\link[=ab_crs]{ab_crs()}}.  Cells belonging to the grid (those covering Alberta)
hold the value 1; cells outside the province are \code{NA}.
}
\details{
This is the default reference grid for \code{\link[=check_alignment]{check_alignment()}},
\code{\link[=resample_to_grid]{resample_to_grid()}}, and \code{\link[=harmonize_crs]{harmonize_crs()}}, so harmonized
layers share one CRS, extent, resolution, and origin by default.

The template is snapped to the lattice of the source grid
polygons, so a cell's raster row and column reproduce its source
\code{GRID_LABEL} (\code{"<row>_<col>"}, row 1 at the north edge, column 1
at the west edge).
}
\examples{
\dontrun{
ref <- ab_grid()
terra::res(ref)
terra::ext(ref)
}

}
\seealso{
\code{\link[=resample_to_grid]{resample_to_grid()}} to harmonize a layer onto this
grid; \code{\link[=check_alignment]{check_alignment()}} to test a layer against it.
}
