% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_points.R
\name{extract_points}
\alias{extract_points}
\title{Extract raster values at point locations}
\usage{
extract_points(x, points, bind = TRUE, ...)
}
\arguments{
\item{x}{A \code{SpatRaster} with one or more layers.}

\item{points}{An \code{sf} or \code{SpatVector} of point locations.}

\item{bind}{Logical; if \code{TRUE} (default), the extracted values
are column-bound to the attributes of \code{points}.  If \code{FALSE},
only the extracted values are returned.}

\item{...}{Additional arguments passed to \code{\link[terra:extract]{terra::extract()}}.}
}
\value{
A \code{data.frame}.
}
\description{
Extracts cell values from \code{x} at each location in \code{points}.
Points are reprojected to the raster CRS if needed (a warning is
issued when this happens).  The returned \code{data.frame} has the
same number of rows as \code{points} with one additional column per
raster layer.
}
\examples{
\dontrun{
library(terra)
library(sf)
r   <- rast(nrows = 10, ncols = 10, crs = "EPSG:4326")
r[] <- 1:ncell(r)
pts <- st_as_sf(
  data.frame(id = 1:3, x = c(-1, 0, 1), y = c(-1, 0, 1)),
  coords = c("x", "y"), crs = 4326
)
extract_points(r, pts)
}

}
